<Page x:Class="VideoOnDemandWPF.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:VideoOnDemandWPF.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="SearchPage">
    <Page.DataContext>
        <vm:SearchPageViewModel/>
    </Page.DataContext>


    <Grid>
        <TextBlock Text="Media Library" FontSize="25" HorizontalAlignment="Center"/>
        <TextBox HorizontalAlignment="Center" Margin="74,44,78,0" TextWrapping="Wrap" Text="{Binding input}" VerticalAlignment="Top" Height="22" Width="148"/>
        <Button Content="Search" HorizontalAlignment="Center" Margin="114,84,111,0" VerticalAlignment="Top" Width="75"/>
        <ListBox ItemsSource="{Binding input}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DataGrid>
                        
                    </DataGrid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
